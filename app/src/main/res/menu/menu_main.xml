<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/loggedUserGroup"
        android:visible="false">

        <item android:id="@+id/myAds"
            android:title="Meus Anúncios"/>

        <item android:id="@+id/loggout"
            android:title="Sair"/>

    </group>

    <group android:id="@+id/unLoggedUserGroup"
        android:visible="false">

        <item android:id="@+id/loggingOrRegister"
            android:title="Cadastrar / Entrar"
            app:showAsAction="always"/>

    </group>

</menu>